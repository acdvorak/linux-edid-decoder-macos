Minimal deps:

```bash
brew install meson ninja pkg-config argp-standalone gettext
```

Full deps:

```bash
brew install meson ninja pkg-config argp-standalone gettext json-c sdl2 sdl2_image qt@6 doxygen
```
